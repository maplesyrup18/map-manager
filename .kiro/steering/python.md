# Rules for Python based development

* Always create a full project structure
* Use the uv package manager
* Create unit tests in a subfolder 'tests'
* Use 'pytest' to create the tests 
* Always create and activate a virtual environment .venv before running any code